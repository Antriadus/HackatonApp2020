import 'package:flutter/material.dart';
import 'package:hack2020/models/planet.dart';

class SpaceMap extends StatelessWidget {
  const SpaceMap(List<Planet> planets);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
